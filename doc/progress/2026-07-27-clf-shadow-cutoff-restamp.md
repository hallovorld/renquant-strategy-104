# clf shadow re-stamp: expected_content_sha256 swap (missing_train_cutoff fix)

## STATUS
delivered

## WHAT
Swap the topdecile_clf_blend_leg content pin sha256:99687a900bee01c4 ->
sha256:6101a9fe5b200900 in active+golden+pinned-test: the artifact was
regenerated by model#83's patched trainer to stamp
effective_train_cutoff_date=2026-04-28 (top-level OPERATIONAL key). The
booster is byte-identical, smoke predictions exactly equal, and
config_fingerprint UNCHANGED — a metadata-only re-stamp, verified against
the pinned runtime read path.

## WHY/DIR
Day-1 shadow health flagged missing_train_cutoff (degraded): the health
contract cannot compute staleness without the field. Operator directive
2026-07-27: fix and rerun until clean.

## EVIDENCE
model#83 (trainer patch + 3 pinned tests, fp-stability included; suite
891/2); regeneration verified: fp equal three ways, prediction
max_abs_diff 0.0, pinned-runtime metadata shows the field.

## NEXT
Merge after model#83; umbrella PR replaces the committed artifact + pin
advance; live sync; NEW promotion receipt for the identity change (the
scorer-identity monitor watches this lane now); tomorrow's session must
show zero degraded reasons.
