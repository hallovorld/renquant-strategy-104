# clf shadow recipe re-stamp: content pin 6101a9fe -> 1e644354

## STATUS
delivered

## WHAT
Content-pin swap in active + golden + shadow_blend (shadow leg AND blend
component[1]) + pinned tests: the artifact regenerated by model#84's
trainer now stamps provenance_schema_version=v1 / recipe_id=
walkforward_only_v1 / required_axis_fields (common#36 classified the keys
OPERATIONAL — hash-preserving). Booster byte-identical, predictions
identical, config_fingerprint unchanged.

## WHY/DIR
The 2026-07-27 e2e probe: both shadows' top_picks NOT ACTIONABLE under the
round-8 required-recipe-schema contract. This clears the clf leg
(PatchTST's clears via its rescore batch). Operator directive: e2e
complete and perfect.

## EVIDENCE
model#84 verification: fp equal three ways; round-8 admission replication
resolves walkforward_only_v1 under both pinned and 0.15.1 common; top-level
diff vs deployed = exactly the three stamp keys.

## NEXT
Merge after model#84; umbrella batch (artifact + strategy pin + common pin
>=0.15.1 20442b6 + snapshot) -> live sync -> receipt -> probe re-verify.
